// Call chalk to format the log message in green color
// const chalk = require('chalk');
import chalk from 'chalk';

console.log(chalk.green('Starting app in dev mode...')); // eslint-disable-line no-console
